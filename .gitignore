# Build outputs (consolidated by the Artifacts Output Layout)
artifacts/

# Fallback in case a project emits locally
[Bb]in/
[Oo]bj/
dist/

# IDE / OS
.vs/
*.user
.DS_Store

# Personal, machine-local dev notes (never shared); matches project-template.
CLAUDE.md
